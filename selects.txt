--- src\app\(main)\accounting\reports\aging-payable\page.tsx
.select('id, number, doc_type, total, balance, status, issue_date, due_date, party_id, party:parties(legal_name, doc_number)
--- src\app\(main)\accounting\reports\aging-receivable\page.tsx
.select('id, number, doc_type, total, balance, status, issue_date, due_date, party_id, party:parties(legal_name, doc_number)
--- src\app\(main)\accounting\reports\cash-flow\page.tsx
.select('type, amount, description, concept')
.select('name, type, balance')
.select('total')
.select('total')
.select('total')
--- src\app\(main)\accounting\reports\purchases\page.tsx
.select('*, party:parties(legal_name, doc_number)
--- src\app\(main)\accounting\reports\sales\page.tsx
.select('*, party:parties(legal_name, doc_number)
--- src\app\(main)\my-payroll\page.tsx
.select('id, number, issue_date, total, status')
--- src\app\(main)\support\tickets\new\page.tsx
.select('id, legal_name')
.select('id, number, doc_type')
.select('id, name')
--- src\app\(main)\treasury\cartera\page.tsx
.select(` id, number, doc_type, total, issue_date, 
.select(` id, number, doc_type, total, issue_date, 
--- src\app\(main)\treasury\page.tsx
.select('id,total,status,due_date,balance')
.select('id,total,status,due_date,balance')
--- src\app\(public)\portal\pago\[id]\page.tsx
.select('id, number, total, due_date, issue_date, status, party_id, tenant_id, tenant:tenants(name)
--- src\features\analytics\services\analyticsService.ts
.select(` product_id, qty, line_total, products!inner (name, sku, cost)
.select('balance')
.select('total, doc_type, status, issue_date')
.select('id, number, doc_type, status, total, issue_date, due_date, party_id, tenant_id')
.select('balance')
.select('amount')
.select('total')
.select('status, shipped_at, delivered_at')
--- src\features\dashboard\services\dashboardService.ts
.select('balance, total, issue_date, status')
.select('*', { count: 'exact', head: true })
.select('id, cost')
.select('product_id, qty, line_total, products(name, sku)
.select('id, number, total, due_date, parties(legal_name)
.select('id, number, doc_type, status, total, issue_date, party:parties(legal_name)
--- src\features\dian\services\dianService.ts
.select('*')
.select()
.select('*, lines:document_lines(*)
.select('number, issue_date, id')
.select('cufe')
--- src\features\dian\services\providerIntegrationService.ts
.select('*, lines:document_lines(*)
--- src\features\documents\services\dianIntegrationService.ts
.select(` *, lines:document_lines(*)
.select()
--- src\features\documents\services\documentService.ts
.select('*, party:parties(legal_name, doc_number)
.select('*, lines:document_lines(*)
.select('id, number, doc_type')
.select('id, number, doc_type')
.select()
--- src\features\logistics\services\logisticsService.ts
.select('tenant_id')
.select('*')
.select(` *, party:parties(legal_name, email, phone)
.select()
.select(` *, carrier:logistics_carriers(name)
.select()
.select('*, items:logistics_shipment_items(*)
.select(` *, carrier:logistics_carriers(*)
.select('status')
.select('*', { count: 'exact', head: true })
.select('carrier:logistics_carriers(name)
--- src\features\notifications\services\notificationService.ts
.select('name, sku, min_stock')
.select('qty')
.select('user_accounts(email, raw_user_meta_data)
.select(` number, total, tenant_id, party:parties(email, legal_name)
.select('*')
--- src\features\payroll\components\EmployeeList.tsx
.select('*, party:parties(*)
--- src\features\payroll\components\PayrollDispersionHub.tsx
.select(` id, issue_date, total, status, 
--- src\features\payroll\components\SettlementForm.tsx
.select('*, party:parties(*)
--- src\features\payroll\services\payrollService.ts
.select('party_id')
.select()
--- src\features\portfolio\services\portfolioAgentService.ts
.select('tenant_id')
.select('*')
.select()
.select(` id, number, total, due_date, issue_date, status, tenant_id, party:parties( id, 
.select('*')
.select('tone')
.select(` *, document:documents(number, total, party:parties(legal_name)
.select(` action_type, document_id, executed_at, document:documents(id, total)
.select('amount, document_id, created_at')
.select('risk_level, average_payment_days, party:parties(id, documents(total, status, doc_type)
.select('auto_escalate_days')
.select('name, email')
.select('tone')
.select(` id, due_date, allocations:payment_allocations(amount, created_at)
.select()
--- src\features\purchasing\services\purchasingService.ts
.select('*, lines:document_lines(*)
.select('party_id, total, status, doc_type')
.select(` ref_doc_id, occurred_at, documents!inner(party_id, issue_date)
--- src\features\sales\services\salesService.ts
.select('*, lines:document_lines(*)
.select()
--- src\features\support\services\supportService.ts
.select(` *, party:parties(legal_name, doc_number)
.select(` *, party:parties(*)
.select('tenant_id')
.select()
.select(` *, author:profiles(full_name, avatar_url)
.select('user_id')
.select('id')
.select()
.select('total, status')
--- src\features\treasury\services\treasuryService.ts
.select('id')
.select('id,name,type,bank_name,account_number,balance,chart_account_id')
.select()
.select(` *, party:parties(legal_name)
.select('id,number,doc_type,issue_date,due_date,total,balance,status')
.select('*')
.select('*, account:treasury_accounts(*)
.select('account_code, tax_name, rate')
.select('account_code, name, rate')
.select('*')
.select()
.select('*')
.select('*, party:parties(legal_name)
.select('*, account:treasury_accounts(*)